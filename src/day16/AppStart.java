package day16;      // day16 폴더안에 들어가 있다.

import day16.view.MemberView;   // day16 -> view 패키지 안에 MemberView 클래스를 가져온다.

public class AppStart {         // AppStart 클래스 정의
    public static void main(String[] args) {    // main 함수 실행

        MemberView.mView.index();   // MemberView클래스 안 mView 객체가 index() 함수를 실행다.

    }   // main end
}   // class end
